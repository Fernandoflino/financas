# Supabase
VITE_SUPABASE_URL=https://vfglcvmcsiwvqtkpjchl.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_LPTetDe1s2wLBLT6nc2kKQ_yI-Pmlj1

# OpenRouter (será armazenado em app_admin.ai_config no banco, criptografado via Vault)
# Não coloque a chave aqui em produção — use o painel admin para configurar
OPENROUTER_API_KEY=sk-or-v1-SEU_TOKEN_AQUI_NAO_COMMITAR

# BRAPI (se houver token de acesso — para uso em testes locais, se necessário)
# BRAPI_TOKEN=seu_token_aqui

# Ambiente
NODE_ENV=development
